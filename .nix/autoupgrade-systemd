Если вариант обновления по времени вам нравится, есть вариант обновлять систему после включения


sudo nano /etc/systemd/system/autoupdate.service

[Unit]
Description=NixOS auto-upgrade after boot
After=network.target

[Service]
Type=oneshot
ExecStart=/run/current-system/sw/bin/nixos-rebuild switch --upgrade --flake /home/username/.nix
RemainAfterExit=yes
[Install]
WantedBy=multi-user.target


Вешаем таймер:

sudo nano /etc/systemd/system/autoupdate.timer
 
[Unit]
Description=Automatic Update when booted up after 5 minutes then check the system for updates every 60 minutes

[Timer]
OnBootSec=5min
#OnUnitActiveSec=60min
Unit=autoupdate.service

[Install]
WantedBy=multi-user.target

sudo systemctl enable /etc/systemd/system/autoupdate.timer

Дать права root пользователю.
sudo chmod o+rx /home/username/.nix
# или точнее:
sudo setfacl -m u:root:rx /home/username/.nix
